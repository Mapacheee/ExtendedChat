name: ExtendedChat
version: ${version}
main: me.mapacheee.extendedchat.ExtendedChatPlugin
api-version: '1.21'
description: ${description}
author: Mapacheee
folia-supported: true
dependencies:
  server:
    MapacheeeLib:
      load: BEFORE
      required: true
    PlaceholderAPI:
      load: BEFORE
      required: false